# Voxel World — Autonomous Evolution Log

Continuous 8-strategy improvement cycles. Each cycle: 8 strategy analyzers → consensus top 3 → implement → verify → deploy to GitHub Pages. Started 2026-06-12, running ~24h or until stopped.

## Cycle 0 — Baseline (2026-06-11)
The game as built: single-file voxel sandbox (~5,030 lines) with four layers — solo engine (chunked Uint8Array world, DDA targeting, AABB physics, canvas texture atlas, day/night, water, touch controls, sparse-diff saves + JSON import/export), RAPP kited neighborhood (PeerJS + broker fallback, sealed rapp-twin-chat/1.0 envelopes, scan-to-join QR, kite mark, host hub relay, snapshots), VoxelFlayer bots (mineflayer-style API, shared physics, A* fenced to loaded chunks, void rescue, /chat twin loops with backoff), vox console CLI (string commands, CDP-safe returns, events since-cursor, headless mode, backquote terminal, safe tp).

Field notes from the launch-day twin fleet (already fixed — do not re-propose): PeerJS default broker fallback (&b= in link), per-tab rappid, action-JSON string coercion, spawn-inside-stone, void rescue, safe tp snap, unloaded-chunk pathing fence, phantom headless players suppressed.

Standing wishes from twin pilots (fair game for future cycles): block-break progress stages; richer observations for LLM brains (cheaper, more spatial); bot memory across saves; waypoints/pings; dig is async with no completion signal in the command form; map command shows terrain only at surface level (caves invisible).

## Cycle 1 — 2026-06-12

Implemented (file now ~5,440 lines; both inline scripts pass `node --check`; no duplicate top-level declarations; no missing element-id references):

1. **Hold-to-mine with crack stages + dig events.** Local-player breaking is now damage-accumulation (`breakState`, hardness table: Leaves 0.18s / Sand 0.30 / Grass+Dirt 0.35 / Wood 0.6 / Stone 0.9) with a 4-stage crack overlay (atlas tiles 9-12, stroked on transparent; one persistent 1.004-scaled box mesh, UVs touched only on stage change, skipped under headless). Release/look-away decays at 3×; completion runs the byte-identical old path (`setBlock` → sparse-diff save → neighborhood `edit` op). Touch: stationary hold past ~280ms mines continuously; quick tap applies `min(1, 0.3/hardness)` so leaves/dirt still one-tap. Bots (`bot.dig`) and `vox dig` keep instant single-call semantics; the ring buffer now gets `dig_progress` (quarter-stage transitions) and `dig_complete` (player, bot, and vox-command paths) — closes two standing wishes.
2. **Adaptive resolution governor + F3 instrumentation.** `renderScale` 0.6–1.0 in 0.1 steps applied as `basePR (= min(DPR,2)) * renderScale`; rides the existing 1s fps tick — drop after 2s under 45fps, raise after 4s over 55, max one step/second, quiet for 3s post-boot / while loading / while panels are open; never touches renderDist or NearestFilter. 'Auto quality' checkbox in settings (default ON for touch, OFF desktop) persisted in the settings blob (`autoQuality`); unchecking snaps back to 1.0. F3 HUD gains frame ms (avg), pixel ratio (eff), mesh ms (last, timed around `buildChunkMesh`); `vox status` now reports `renderScale`/`autoQuality`. Render call additionally guarded by `!document.hidden`.
3. **First-90-seconds onboarding coach.** `voxel-world-onboard-v1` key gates a 5-step bottom-center hint card (LOOK → MOVE → BREAK → PLACE → TOWER, IS_TOUCH-branched copy, only Skip takes pointer events) that ALSO runs for scan-to-join QR arrivals — the previously zero-guidance path. Counters live at the real action sites (break completion, `doPlace` success) and count from session start so out-of-order actions retroactively clear steps; hidden (never advanced) while panels/chat/terminal are open; suppressed under `?headless=1`/`?console=1`; private-mode degrades to once-per-session; coach replaces the auto help modal on first run and sets HELP_KEY on completion/skip. Joiners also get a "You're in <host>'s world" toast (host name from the snapshot's first roster entry).

Cycle 1 fixer pass (post-verification, same day): two real regressions fixed.
(a) Touch quick-tap math was broken: 0.3/0.35 = 0.857 meant grass/dirt never one-tapped, and 3/s decay between taps made stone un-tappable. Fix: tap constant bumped to 0.36 (grass/dirt/sand/leaves = one tap, wood 2, stone 3) plus a 400ms post-tap decay grace window (`lastTapT` guard in `decayMineDamage`) so consecutive taps actually accumulate. Hold-to-mine, bot dig, and vox dig untouched.
(b) The `!document.hidden` render guard was dead code for the hidden-tab promise — rAF stops entirely in hidden tabs, freezing twins/bots/broadcasts. Fix: a `hiddenTickTimer` setInterval (250ms, mirroring the `voxSetHeadlessTimer` pattern) drives `gameTick` while the tab is hidden and non-headless, cleared on return to visible. Note: browsers throttle background timers to >=1s, so hidden ticking is coarse (dt clamp bounds each step) — a Blob-worker clock would beat throttling if a future cycle needs full-rate hidden ticking.
(c) Interaction bug between (b) and the governor: hidden ticks render zero frames but still feed the fps accumulator (dt clamped to 0.1 → fpsVal reads as a constant 10), and `qualityGovernorTick` had no hidden guard, so with autoQuality on a hidden tab ratcheted renderScale to the 0.6 floor and the user came back to ~16-20s of blur. Fix: `|| document.hidden` added to the governor's pause condition (resets both streaks), and the visibilitychange return-to-visible branch now flushes `fpsFrames/fpsAcc/fpsLowStreak/fpsHighStreak` so the first visible 1s window (F3 HUD, `vox status`) measures only real rendered frames.

Field notes for future cycles:
- Crack overlay is local-only cosmetic v1 — remote players do not see partial mining progress. A `dig_progress` op on the wire (throttled like `pos`) would fix it if anyone cares.
- Desktop quick-click no longer breaks anything instantly (even leaves need 0.18s of hold) — intentional per spec; if playtests hate it, give desktop click the same `min(1, 0.3/hardness)` tap chunk as touch.
- Touch hold-to-mine starts at 280ms; a tap landing in the 280–350ms window gets both the held damage and the tap chunk (tiny double-count, accepted so quick taps can never fail to break leaves).
- The governor reads `fpsVal` (already a 1s average) with streak counters rather than a separate rolling buffer — same hysteresis, less state. Headless is a hard no-op (the 20Hz headless tick would otherwise read as "low fps" and pointlessly downscale).
- Existing players (HELP_KEY set, onboard key unset) will see the coach once after this deploy — accepted per spec; it self-dismisses in ~90s of normal play and Skip is permanent.
- Standing wishes still open: richer/cheaper LLM observations; bot memory across saves; waypoints/pings; map shows surface only (caves invisible).

### PRIORITY-SECURITY for cycle 2 (consensus chair: this MUST take one of the three slots)
Verified by cycle-1 devils-advocate: imported/persisted tether+cloud twins auto-start their network loop — world import (~line 1852) and boot re-hatch (~line 4036) call vfStartTwinLoop, which POSTs full world observations to the twin's arbitrary `url` (~line 3752) with no validation or consent. A malicious world-export JSON can therefore exfiltrate gameplay data to an attacker URL the moment it is imported. Fix: imported/re-hatched network twins land PAUSED with a visible badge; resuming requires an explicit user click backed by a per-URL allowlist persisted in localStorage (localhost defaults allowed); never auto-start a network twin whose URL has not been approved on this device.

### Cycle-1 runners-up worth re-weighing (not yet implemented)
Pings & waypoints (sealed 'ping' op — remember the host relay allowlist at ~2499); async chunk meshing off the setBlock path (dirtyMeshSet drained in processStreaming); floating dynamic joystick + touch comfort settings; material-aware footsteps/landing/splash audio; remote-edit bounds (radius + cap + rate limit, anti-grief); joiner-side broker retry with backoff; 3D cave carving (prereq for ores; pairs with the caves-invisible map wish).

## Cycle 2 — 2026-06-12
(The orchestration script labeled this run "cycle 1" of a fresh loop; in this log's numbering it is cycle 2 — it implements exactly the cycle-2 slate mandated above. File now ~5,940 lines; both inline scripts pass `node --check`; no duplicate top-level declarations; no missing element-id references; 48/48 functional checks pass in a Node harness run against the shipped code extracted verbatim.)

1. **Consent gate + import validation for network twins (the PRIORITY-SECURITY mandate).** `isAllowedTwinOrigin(url)` (localhost/127.0.0.1 any port always allowed; otherwise a persisted origin allowlist at `voxel-world-twin-url-allow-v1`, try/catch everywhere, storage failure degrades to localhost-only) now gates `vfHatchTwin`: approved origin → live; `fromImport` → paused with an amber badge, zero prompts, zero network; interactive hatch → one `window.confirm` showing the exact origin, accept persists. All three import funnels (boot re-hatch, world import, vox import) route through `vfImportRoster`, which also hard-drops malicious roster records via `validateTwinRecord` (no usable name / bad mode / non-http(s) or >200-char url — `javascript:` dies here) and coerces the rest; VF_MAX_BOTS enforced across existing+incoming; duplicate names skipped. Per-row Resume button (allow-or-confirm), `vox('resume <name>')` (never prompts; `origin not approved` otherwise), `vox('twinallow <origin>')` (operator full-trust). Roster JSON shape unchanged — 'paused' is derived at hatch, never persisted; `validateSave` additionally rejects a save whose `twins` is present but not an array.
2. **Sealed waypoint pings (op 'ping' on the rapp-twin-chat wire).** Middle-click (pointer-locked), a touch PING button, and `vox('ping [here|look|danger]')` ping the crosshair DDA target (fallback: 12 blocks along the view ray). Leaf→host sealed, host fan-out sealed, and — the trap the proposal warned about — `'ping'` added to the host hub-relay op allowlist so leaf→leaf propagates. Strict receive validation (finite+integer clamp, kind whitelist, 1s per-sender rate limit), one live marker per sender in `Neighborhood.pings` (replace on re-ping, 16 cap evicting oldest, 8s timestamp expiry per frame, full geometry/material/CanvasTexture disposal incl. `nbResetSession`). Marker = ring+name billboard sprite in the sender's color + a depth-test-free beam. Solo ping = personal local marker; headless skips all scene objects but still emits the `ping` vox event + sys chat line.
3. **Deterministic 3D cave carving + map y-slice.** `valueNoise3`/`fbm3` (ihash with a `Math.imul(y, 0x9E3779B1)` salt — pure in (seed,x,y,z)); `genChunk` carve pass after columns / before trees (edits overlay still last, so player edits win): STONE-only band `|fbm3(seed^0xCA7E, …)-0.5| < min(0.034, 0.018+depth*0.0008)`, oceans (h<SEA) skipped, y≤1 bedrock intact, WATER untouched; fbm3 sampled only on a WORLD-ALIGNED 4-block lattice (5×5×17/chunk) and trilinearly interpolated, so borders are seamless and chunk bytes are load-order independent (verified). `vox('map [r] [y]')` renders a horizontal slice at y (clamped 0..63) with '.' for air and entities marked only within ±2 of the slice; without y the output is byte-identical to before (diffed against git HEAD's implementation in the harness — text and return JSON byte-equal). Return adds `slice` key only in slice mode.

**Spec deviations (deliberate, measured):**
- The proposal's `t ~0.06` band measured 25-37% of underground stone carved (a value-noise fbm clusters near 0.5, so the band catches far more volume than intended). Shipped `0.018+depth*0.0008 (cap 0.034)` → 7.5-18% across 6 seeds, Minecraft-ish density.
- "Cave mouths appear where caves intersect exposed hillside/cliff stone" is unsatisfiable here: this terrain's adjacent-column height delta NEVER exceeds 1 (measured), and every column wears a 4-block non-stone cap, so a stone-only band can never daylight. Shipped a separate sparse deterministic mouth pass (`caveMouthAt`, p=0.0045/column): hillside grass columns only (never beaches/flat tops/under trees), and only where `caveFieldAt` — the same lattice-interpolated field the carve pass uses — CONFIRMS a cave 4-7 blocks below (no blind pits). Mouth shafts are margin-scanned like trees so they cross chunk borders seamlessly. Measured: 2-9 mouths within ~96 blocks of spawn (seed 1337 nearest ≈101), zero flat-top holes on every seed tried.
- Carve cost measured ~0.06ms/chunk in Node (~0.24ms at 4x throttle) — well inside the 8ms streaming budget.

Field notes for future cycles:
- `window.confirm` in headless Chrome auto-dismisses (returns false), so a CDP-hatched non-localhost twin lands paused — the operator path is `vox('twinallow <origin>')` then `vox('resume <name>')`. Working as intended, but worth knowing before filing a "twin won't start" bug.
- findSpawn is unchanged per spec, so ~0.5% of fresh worlds may spawn on a mouth column and drop the player into a cave (no fall damage exists; you dig out). If playtests complain, exclude `caveMouthAt` columns in findSpawn.
- Ping markers are scene-only and rate-limited per sender, but a host relaying N leaves' pings still toasts on every accepted ping — if 8-peer sessions get noisy, gate the toast (keep the chat line + event).
- Twin roster exports now round-trip through validateTwinRecord on import; records that legacy exports carried with junk fields (string intervals etc.) are coerced rather than dropped — only unsafe ones die.
- Standing wishes still open: richer/cheaper LLM observations; bot memory across saves; block-break progress visible to remote players (dig_progress op); async chunk meshing off the setBlock path; remote-edit bounds (anti-grief); ores (caves now exist to put them in).

## Cycle 3 — 2026-06-12
(The orchestration script labeled this run "cycle 1" of a fresh loop; in this log's numbering it is cycle 3. File now ~6,230 lines; both inline scripts pass `node --check`; no duplicate top-level declarations; no missing element-id references; 8/8 functional checks pass in a Node harness extracted verbatim from the shipped code. This run finally lands the long-standing "async chunk meshing off the setBlock path" wish from cycles 1-2.)

1. **Async/throttled chunk remeshing off the setBlock path.** Module-level `dirtyMeshSet` (Set of chunkKeys) drained nearest-first in `processStreaming()` under the same 8ms in-game budget. `setBlock` gained a 6th `localEditFast` arg: only the local player's own break/place (`completePlayerBreak`, `doPlace` pass `true`) still remeshes ITS chunk synchronously for instant input feel; every remote (`nbApplyRemoteEdit`), bot (`bot.dig/place`), and vox-command edit — plus the up-to-4 border neighbors for ALL edits — defers to the set. Set keying coalesces a bot's tight setBlock loop to one rebuild/chunk/frame (kills the per-tick hitches). Drain deletes a key before meshing (no re-mesh within a drain), silently drops keys for unloaded chunks, and skips chunks already rebuilt this frame via meshQueue (`solidMesh && !dirty`). `disposeAllChunks` clears the set so a world reload never meshes stale keys. F3 HUD gains a `dirty N` counter. Zero change to face-visibility / record / save / edit-op ordering — only WHEN a rebuild happens.
2. **Neighborhood trust fixes (two guards, one slot).** (A) `from_rappid` binding: `nbHostOnData` now rejects any ready-peer envelope whose `env.from_rappid !== entry.rappid` BEFORE both local dispatch and the hub relay — kills forged-identity `bye` (avatar eviction), `bot-claim`/`bot-release` (claim theft; those handlers key entirely off `from_rappid`), and chat/ping impersonation. `nbHostWelcome` also rejects a hello whose `from_rappid` collides with an already-ready entry's rappid (closes the conn). Stable per-tab sessionStorage rappid means reconnect never trips the bind. (B) `MAX_EDITS = 250000` (~10MB JSON) in `validateSave`, checked BEFORE the per-key loop, so a giant `edits` object fast-fails (~20ms) instead of hanging the tab; `nbApplySnapshot` already funnels through `validateSave`, so malicious host snapshots and imported JSON are covered with zero extra code. No wire/shape change.
3. **View-bob + landing dip + jump lift + material-aware footstep audio.** A `camFx = {dy,dx,roll}` accumulator applied AFTER the eye pin (reads `player.pos/vel/onGround`, writes ONLY `camera.position/rotation` + the WebAudio graph — never `player.pos`, so DDA targeting stays crosshair-locked). `bobPhase` advances on horizontal speed only while `onGround && speed>0.5 && !fly`; sin-driven vertical bob + lateral sway + tiny roll, amplitude-scaled by speed and hard-clamped (|dy|,|dx|<0.06, |roll|<0.012). Landing dip springs back critically-damped (~0.18s, magnitude ∝ impact |vel.y|) on the onGround false→true edge; small upward kick on jump. `playMoveSound` adds procedurally-synthesized footstep/land/splash SFX (noise burst + filtered tone, NO files): grass/leaves soft high-passed rustle, sand drier mid, stone sharp click, dirt/wood muffled thud, ±12% per-step pitch jitter, ≥370ms hard rate cap; land thud heavier on stone; splash on the water-entry edge. A 'Camera motion' settings checkbox (default ON, auto-OFF under `prefers-reduced-motion`, persisted next to `autoQuality`) gates the visual bob/dip/lift only; footsteps stay on `soundOn` independently. Fully skipped under `?headless=1` and all movement audio is silenced on hidden ticks; a safe-tp / world-load suppresses the dip for 2 frames so a positional jump never reads as a landing.

Field notes for future cycles:
- Border seams on a deferred edit resolve in ~1-2 frames thanks to nearest-first draining; the player's own chunk is always synchronous so the edit you make never flickers. If a future cycle ever sees a persistent seam, check that `markNeighbor` set the neighbor's `dirty` flag (the drain's "already meshed" guard skips clean chunks).
- The `from_rappid` bind tightens only what the HOST accepts — old clients are unaffected on the wire (no new ops). The `bye` payload still carries `p.rappid`, but a ready peer can no longer impersonate another's `from_rappid`, and `bye` isn't in the relay allowlist, so a forged `bye` can't propagate to other neighbors either.
- View-bob reads the PLAYER entity even in `vox('become <name>')` mode (the camera always follows `player.pos`), so a console operator "being" a twin still gets the bob from the player's own motion — harmless, but worth knowing.
- Footstep cadence is driven by `bobPhase` which keeps advancing even when Camera motion is OFF (only the camera OFFSET is gated), so toggling motion off silences the bob but NOT the steps — that's intentional per spec (steps follow `soundOn`).
- Standing wishes still open: richer/cheaper LLM observations; bot memory across saves; block-break progress visible to remote players (dig_progress op); remote-edit bounds (radius + cap + rate limit, anti-grief); ores in the caves; joiner-side broker retry with backoff; map shows surface-or-slice but no auto cave overview.

## Cycle 4 — 2026-06-12
(The orchestration script labeled this run "cycle 1" of a fresh loop; in this log's numbering it is cycle 4. File now ~6,484 lines; both inline scripts pass `node --check`; no duplicate top-level declarations — 427 scanned, 0 dup; no missing element-id references; functional harness extracted verbatim from the shipped pure functions: 64/64 ore+validateSave+findSpawn checks pass, 12/12 obs-shape checks pass. This run finally lands the long-standing "ores in the caves" and "richer/cheaper LLM observations" wishes cited in every prior cycle's field notes.)

1. **COAL/IRON/GOLD ores in the caves (atlas 8x8 + validateSave ceiling bump).** Three depth-gated ore blocks appended at `B.COAL_ORE=8 / IRON_ORE=9 / GOLD_ORE=10` (id 7 WATER unchanged; nothing shifts). The texture atlas grew from 4x4 to 8x8 (`ATLAS_COLS=ATLAS_ROWS=8`) with NO renumber and NO UV-math change — every tile→cell conversion (tileUV/fillTile/crackTile/hotbar) derives the cell from the linear index via the same constants, so the existing 0-12 tiles (blocks + the four crack-overlay tiles 9-12) stay self-consistent as they move to their new `floor(N/8)` rows; ores append at 13/14/15. The 3 ore tiles draw procedurally (stone-speckle base reused from a named `stonePixel` fn, overlaid with a 3-5 nugget cluster in the ore color) — no external image files. Generation runs a SEPARATE-salt (`worldSeed ^ 0x0CE5`), FINER-frequency (`ORE_FREQ=0.11` vs caves' 0.05) field on the SAME world-aligned CAVE_STEP lattice + trilinear interp as `caveFieldAt`, so veins cross chunk borders seamlessly and chunk bytes stay load-order independent; it runs AFTER `carveCaves` so ores only seed in REMAINING stone (never carved air), over the stone band `[2 .. h-4]` (never bedrock y<=1, never the surface cap, never water). Thin level-set bands (coal 0.012 / iron 0.0085 / gold 0.006) make small clustered veins, not scattered singles; depth-gated coal (y>=12) / iron (6..20) / gold (2..10), deepest-first so gold wins the deep core and is rarest. Measured 0.13-0.54ms total gen/chunk and ~0.14ms ore-pass/chunk across 6 seeds — well inside the 8ms streaming budget. `validateSave`'s edit-id ceiling widened from `v > 7` to `v > MAX_BLOCK_ID (=10)` (single source of truth) so a broken+replaced ore round-trips through save/export/snapshot/`nbApplyRemoteEdit` — pure widening, no prior save can newly fail. Ore hardness (coal 1.1 / iron 1.5 / gold 1.4) is tougher than STONE 0.9 so hold-to-mine feels rewarding; break particles use the ore colors; `BLOCK_NAMES` carries the ore names so vox/bot observations and `vox map` read them automatically.
2. **Richer + cheaper twin observations (additive, backward-compatible).** `vfObservation()` — the single builder feeding brainstem twins, `vox('obs')`, and `vox('become')` — gained four cheap in-memory fields: (1) `lookingAt` = the SAME DDA crosshair ray cast from the subject's eye along (yaw,pitch) up to REACH, returning `{x,y,z,block,face,dist}` or null (try/catch so a bad cast never breaks the 20Hz loop) — the "dig what I'm looking at" datum that pairs directly with the new ores; (2) `pings` = `Neighborhood.pings` within ~64 blocks mapped to `[{name,kind,pos,distance}]`, nearest-first, capped at 4 (connects cycle-2 pings to twin perception); (3) `groundProfile` = `{standingOn, headroom, drop, aheadDrop}` (~20 getBlock reads, all loop bounds clamped) so non-spatial LLM brains get a cliff/hole/ceiling sense and stop walking into void rescue; (4) `aheadStrip` = block names at +1/+2/+3 ahead at eye level. CHEAPER: `nearbyPlayers`/`nearbyBots` capped to the 6 closest within ~64 blocks (was UNCAPPED) — measured a busy 20-player/12-bot scene drops from 1585 to 923 bytes (-662) even WITH the new fields, while the empty common case grows a modest +136 bytes. STRICTLY ADDITIVE: `groundBelow` (now an alias of `groundProfile.standingOn`) and `blocksAhead` (deprecated, code-commented for a LATER cycle's removal) and `worldSeed` are all KEPT so existing personas/CDP scripts never hard-break. Zero save/wire change — observations are local-only. All numbers rounded/clamped, arrays bounded → CDP returnByValue-safe.
3. **findSpawn excludes cave-mouth columns + clean-landing guard (the cycle-2 deferred defect).** The spiral accept predicate now also requires `!caveMouthAt(gx,gz)` so a fresh world never spawns the player onto a mouth shaft and drops them into a cave, plus a deterministic `spawnHeadClear(gx,gz,h)` confirming no NEIGHBORING tree's overhanging leaf canopy occupies (h+1)/(h+2). Crucially, findSpawn runs BEFORE any chunk is generated, so `getBlock()` can't be used for the head check (it reads AIR for every unloaded chunk and would silently always-pass) — `spawnHeadClear` instead recomputes the generator's exact 5x5-minus-corners / plus / apex tree-leaf footprint, staying a PURE fn of (seed,coords). The (0,0) fallback is UNCHANGED. Verified across 200 seeds: the new spawn is NEVER on a mouth column; for seeds 1-12 the spawn didn't even move (their (0,0) r=0 spawn was already mouth-free and canopy-clear), confirming a narrow low-risk change. SAVED/imported worlds load `playerPosition` and never call findSpawn, so they are byte-identical to before; `validateSave` untouched.

Field notes for future cycles:
- The 8x8 atlas left tiles 16-63 unused — plenty of headroom for future block types or per-block crack variants. The "no-renumber" property holds for ANY future grid resize as long as every tile→cell conversion keeps deriving from `ATLAS_COLS`/`ATLAS_ROWS` (it does); a future cycle adding a block just appends an index and a `BLOCK_TILES` entry.
- Ores are pure terrain: they never enter `edits` unless a player breaks+replaces one. A future "ore drops / inventory" feature would still ride the same single `MAX_BLOCK_ID` ceiling — bump it once and every path (save/export/snapshot/wire) follows.
- Ore depth bands overlap (iron 6..20 vs gold 2..10 vs coal 12..) but the deepest-first `continue` chain means each stone cell becomes at most one ore; if a future cycle wants layered/biome ores, split the field salt per ore instead of sharing one lattice.
- `blocksAhead` is now formally DEPRECATED (code comment) but still emitted — a LATER cycle can remove it once no live persona/CDP script keys off it. `groundBelow` is a cheap alias and can stay indefinitely.
- `lookingAt` reuses the player/bot `ent.eye` (1.62) and `ent.pitch`; the `vox('become')` player-shim passes `player` as `ent`, which has both, so "being" a twin produces a valid `lookingAt` from the player's own crosshair.
- The empty-world obs is +136 bytes vs pre-change because the spec mandated KEEPING the deprecated `blocksAhead` AND adding four richer fields — a strictly-smaller common case is impossible without removing a field the spec forbids removing. The capping win is real and large precisely where token cost actually hurts (many entities).
- Standing wishes still open: bot memory across saves; block-break progress visible to remote players (dig_progress op); remote-edit bounds (radius + cap + rate limit, anti-grief); joiner-side broker retry with backoff; ore drops / a real inventory; an auto cave-overview map mode; ores are non-craftable for now (not in PLACEABLE) — a future cycle could let players place mined ore.

## Cycle 5 — 2026-06-12
(The orchestration script labeled this run "cycle 1" of a fresh loop; in this log's numbering it is cycle 5. File now ~6,540 lines; both inline scripts pass `node --check`; 245 top-level declarations scanned, 0 duplicates; 90 ids defined, 7 `getElementById` refs, 0 missing. Standalone Node harnesses: desktop-quick-click break counts 8/8; remote-twin caps 6/6; mesh geometry parity at committed-Float32 representation 40/40 seeds byte-identical incl. water + extent-skip. This run closes the cycle-1 field-note item on desktop quick-click and hardens the lone ungoverned network ingress.)

1. **Desktop quick-click instant-mine + `voxConsoleBotName` cleanup (two log-verified defect fixes).** The mousedown button-0 EDGE now fires the SAME quick-tap chunk as touch (`applyMineDamage(hit, min(1, 0.36/blockHardness(hit.id)))` + `lastTapT` stamp, panelOpen-guarded), so one desktop click one-shots leaves/grass/dirt/sand, 2 wood, 3 stone — the 400ms `lastTapT` decay grace gates it exactly like touch so a held click never double-counts (the `updateMining` hold path still accrues `dt/hardness` for sustained holds). `completePlayerBreak` is byte-identical (save/edit-op/dig_complete unchanged); bot.dig and vox dig keep instant single-call semantics. Separately, `vfRemoveBot` now nulls `voxConsoleBotName` at the top (matching `voxCmdImport`), so removing the console twin via the per-row button or `voxelflayer.remove()` no longer leaves a dangling pointer — `vox status`/`vox bots` stop reporting a stale `consoleTwin` and a headless operator resumes broadcasting presence (the `if (voxHeadless && voxConsoleBotName) return;` guard in `nbSendPos` no longer mis-fires).
2. **Cap & rate-limit remote twin avatars (vfRemoteBotPos flood / GPU-texture leak).** Three coupled guards in the `if (!rb)` new-key branch of `vfRemoteBotPos`, applied ONLY when allocating a NEW key so well-behaved peers' existing twins keep lerping smoothly: (a) global cap `vfRemoteBots.size >= VF_MAX_BOTS*4` (=32); (b) per-owner cap (count entries whose `.owner === env.from_rappid`, refuse at `>= VF_MAX_BOTS`); (c) per-sender new-allocation rate limit (`vfRemoteNewRx` Map, 4 new keys / 1000ms window, mirroring `nbPingLastRx`). All guards run BEFORE `vfMakeBotAvatar`/`scene.add` so a refused allocation never half-builds a Group or leaks a CanvasTexture; the map is cleared in `vfOnSessionReset` (called by `nbResetSession`) so a world reload/session reset starts clean. Zero wire/shape change — old clients send pos as before; a flooding client gets throttled at the receiver. The remote-twin ingress was the lone uncapped receive path (edits/pings/local bots were already bounded).
3. **Typed-array geometry builder + per-chunk vertical-extent skip in `buildChunkMesh`.** PART A: replaced the growable `pos/nor/uv/ind` JS-number arrays + `.push()` with a count-pass over visible solid faces, then index-writes into four MODULE-LEVEL reusable scratch typed arrays (`scratchPos/Nor/Uv` Float32, `scratchInd` Uint32 — a dense chunk reaches ~196k verts, far past Uint16's 65535) grown geometrically; the BufferGeometry commits a right-sized `.slice()` so the attribute OWNS its buffer and the scratch is free for the next chunk (no retained-scratch aliasing). Exact emission order + winding (`base,base+1,base+2, base+2,base+1,base+3`) preserved byte-for-byte; water stays on the old growable path (few faces). PART B: `genChunk` records `chunk.yMin`/`chunk.yMax` (solid Y-span, WATER counts as non-air, via a final-pass `chunkExtent` scan over the post-edits data); `setBlock` widens the span on edits that cross it (breaking never narrows — a stale-high yMax just scans extra all-air rows, never a visibility error); both the count and build loops now iterate `ly = yMin..yMax` instead of `0..WORLD_H`, skipping the always-air upper layers (~40-50% of surface chunks). Pure rendering change — no save/wire/edit-op change; verified pixel-identical (committed-Float32 geometry equality across 40 random chunks incl. floating blocks above the surface). F3 'mesh ms (last)' should visibly drop.

Field notes for future cycles:
- The mesh-parity harness initially "failed" comparing raw `pos`/`uv` arrays: the OLD path stored plain JS Float64 numbers that `Float32BufferAttribute` rounded at commit, while the NEW path stores Float32 immediately. Both upload IDENTICAL Float32 to the GPU — compare at the committed-Float32 representation, not the pre-commit JS numbers, or you'll chase a phantom diff.
- `chunkExtent` is a full-buffer scan at genChunk time (one-time per chunk, cheap with the per-column `break`). If a future cycle wants to also TIGHTEN yMax after breaking the top block (to reclaim the skipped-rows win), do it lazily in the drain, not in `setBlock` (a re-scan per edit would cost more than the few extra air rows it saves).
- The remote-twin caps are receiver-side only; a future "twin trust" feature could additionally require the owner's twins to be in the host's roster before allocating any avatar (today any neighbor's pos-with-`.bot` spawns a visual up to the cap). The per-owner cap (`VF_MAX_BOTS`) already bounds the blast radius.
- `voxConsoleBotName` is declared (~line 4982 of the module script) AFTER `vfRemoveBot` (~line 4347), but every `vfRemoveBot` call site is inside a runtime function (`vfImportRoster`, UI click handlers, `voxelflayer.remove`, `vox` commands) — never top-level — so the forward reference is past the TDZ by call time. `node --check` confirms no syntax issue.
- Standing wishes still open: bot memory across saves; block-break progress visible to remote players (dig_progress op); remote-edit bounds (the cap/rate-limit pattern here is a template for an anti-grief edit fence); joiner-side broker retry with backoff; ore drops / a real inventory; an auto cave-overview map mode.

## Cycle 6 — 2026-06-12
(The orchestration script labeled this run "cycle 1" of a fresh loop; in this log's numbering it is cycle 6. File now ~6,876 lines; both inline scripts pass `node --check`; 472 top-level declarations scanned, 0 duplicates; 90 ids defined, 7 `getElementById` refs, 0 missing. Three standalone Node harnesses extracted verbatim from the shipped code: pure-fn caps/fence/ceiling 30/30; integrated edit-handler+host-relay+vfExecuteOp 24/24; join-watchdog+single-fallback 24/24 — 78/78 total. This run finally closes THREE of the longest-deferred wishes: the anti-grief edit fence (deferred cycles 1-5), the joiner-side broker watchdog (cycles 1,3,5), and persistent bot/twin memory (THE wish cited in every cycle 0-5 field note).)

1. **Anti-grief fence on the remote `edit` op (per-sender rate-limit + MAX_EDITS ceiling, host-enforced before apply AND relay).** The `'edit'` handler now takes `(p, env)` and runs `nbEditFencePass(env.from_rappid)` BEFORE any setBlock/recordEdit: a per-sender token bucket (`nbEditRx`, **30 tokens/sec sustained, 60 burst** — modeled on `nbPingLastRx`/`vfRemoteNewRx`) that DROPS over-budget edits; local/solo identity (`from_rappid === Neighborhood.rappid`, or no rappid) is never fenced. A HARD CEILING then refuses any edit that would add a NEW edit key once `Object.keys(edits).length >= MAX_EDITS` (250000, the single source of truth `validateSave` uses) — an overwrite of an existing key never counts toward growth and is always allowed. The host stamps `nbEditLastAccepted` in the handler (default false; true only on apply) and the hub-relay block reads it, so a refused edit is neither applied locally NOR forwarded to other leaves. `nbEditRx` is cleared in `nbResetSession`. Wire shape unchanged (old clients send `edit` as before, throttled at the receiver); solo/bot-fleet edits ride the LOCAL path (`nbOnLocalEdit`) and are never fenced.
2. **Joiner-side broker watchdog + single alternate fallback (kills the silent QR scan-to-join hang).** `nbJoinWith` now wraps its connection lifecycle in `nbCreatePeer`'s proven `settled` discipline: a 13s PRIMARY watchdog (broker peer never opens), an 8s SECOND-STAGE watchdog armed when peer `open` fires (broker up but host/conn never answers), both cleared the instant the sealed handshake's `conn 'open'` lands so a connected session is never torn down. A terminal broker-unreachable error OR the primary watchdog firing triggers ONE alternate-broker retry (`triedAlternate` guard — at most one, never a storm); a clean `peer-unavailable` (host-gone on a REACHABLE broker) does NOT fall back. Failure surfaces `'Could not reach the kite — is it still flying?'` + toast within ~13s instead of hanging forever; the failed peer is `destroy()`ed (no leaked background retry). Pure join-path hardening — no wire/op/save/host change; `vox('join')`'s 30s `voxWaitFor` still wraps it and just fails faster/louder now.
3. **Persistent bot/twin memory (saved + observation-fed) with named waypoints.** `bot.memory = { facts:[], waypoints:{}, stats:{dug,placed,chats} }` seeded in `vfSpawnBot`. New vox ops in the shared `vfExecuteOp` (used by twins, neighborhood bot-act, and vox act): `{op:'remember',key,value}` (FIFO, cap 24, ≤120-char), `{op:'waypoint',name,x,y,z}` (cap 16, finite-integer-clamped, drop-oldest), and `{op:'goto',waypoint:'home'}` resolving a named coord (chats `no such waypoint` when missing). Stats auto-increment at the REAL action sites in `bot.dig`/`bot.placeBlock`/`bot.chat` so every path counts. `vfObservation` gains a bounded `memory` field (`facts.slice(-8)` + waypoints + stats, all CDP-returnByValue-safe, strictly additive). Persistence: `vfRosterData` emits `vfSanitizeMemory(bot.memory)`; `validateTwinRecord` coerces an OPTIONAL `memory` key through the same sanitizer (drops >24 facts / >16 waypoints / non-integer coords, clamps ≤120-char values and non-negative integer stats — mirroring the MAX_EDITS defense); `vfHatchTwin`/`vfImportRoster` rehydrate it onto the bot. Twin.history (full prompts) is NEVER persisted. Built-in bots with no twin record keep memory in RAM only (acceptable per spec).

**Implementation notes / deviations:**
- Edit-fence rate constants (30/s sustained, 60 burst) were chosen well above real cadence: a bot's tight setBlock loop already coalesces to one remesh/chunk/frame, and human tower-spam tops out far below 30/s. A 200-edit/100ms flood in the harness caps at ~62 accepted (60 burst + ~2 refill) with the overflow neither applied nor relayed; a slow 1/sec cadence runs unthrottled forever; the full burst always gets through.
- The optional radius/bounds fence was left OUT of scope this cycle (per spec) — it risks surprising far-traveling neighbors. Rate-limit + ceiling shipped only.
- The waypoint-name sanitizer reuses `validateTwinRecord`'s pattern (strip control chars + angle brackets) (control chars + angle brackets); written as escaped unicode (not literal control bytes) so the source stays grep-able and portable.
- The host relay needed a flag (`nbEditLastAccepted`) rather than moving the fence into `nbHostOnData`'s edit branch, because the registered `'edit'` handler runs inside `nbDispatch` (called BEFORE the relay block); the flag is the cleanest single-source decision both apply and relay read.

Field notes for future cycles:
- The edit fence is the template the cycle-1..5 field notes kept asking for. A future RADIUS bound would slot in right after `nbEditFencePass` returns true (before the ceiling check) — but remember far-traveling neighbors: bound it to a generous box around each sender's last-known pos, not a fixed spawn radius.
- `nbEditLastAccepted` is a module-level scratch flag, valid only for the synchronous window between the edit handler running and the relay block reading it (both inside one `nbHostOnData` turn). It is harmlessly set-and-unread on a leaf (leaves don't relay). Don't read it anywhere else.
- The joiner watchdog falls back on the broker-error allowlist `NB_JOIN_BROKER_ERRS` (network/server/socket/etc.) but NOT on `peer-unavailable`. If PeerJS ever renames an error type, a broker-unreachable failure would silently skip the fallback and just fail terminally — re-check the set against the PeerJS version pinned in `NB_BROKER_CONFIGS` if joiners report "could not reach the kite" on a broker that's actually up.
- Memory caps are enforced on BOTH the write side (`vfExecuteOp`) AND the import side (`validateTwinRecord`/`vfSanitizeMemory`), so a runaway brain and a hostile export are both bounded. If a future cycle adds richer memory (e.g. typed facts), keep the sanitizer the single choke point every import funnel (boot re-hatch / world import / vox import) already routes through.
- `obs.memory.facts.slice(-8)` keeps the observation bounded even at the 24-fact cap; if a brain needs the full 24 it can read them via `vox` but the per-turn token cost stays flat.
- Standing wishes still open: block-break progress visible to remote players (dig_progress op, throttled like pos); ore drops / a real inventory; an auto cave-overview map mode; the optional remote-edit RADIUS bound (rate-limit + ceiling now shipped); a Blob-worker clock for full-rate hidden-tab ticking (browsers throttle background timers to ≥1s).

## Cycle 7 — 2026-06-12
(The orchestration script labeled this run "cycle 1" of a fresh loop; in this log's numbering it is cycle 7. File now ~6,946 lines, +70 net; both inline scripts pass `node --check`; 455 top-level declarations scanned, 0 duplicates; 92 ids defined, 0 `ui.X` / `getElementById` refs without a matching element. This run is a polish+correctness slate: it activates dead safe-area CSS, gives touch its missing tactile/visual hit-confirmation, and closes a verified twin-roster state/security leak on neighborhood leave.)

1. **Activated dead safe-area insets (`viewport-fit=cover`) + landscape side-inset reflow.** The viewport meta gained `viewport-fit=cover`, which alone activates the ~14 existing `env(safe-area-inset-top/bottom)` calc()s already written into the stylesheet (hotbar, joystick, jump/down/mode/ping cluster, HUDs, toast, chat, terminal) — they had been silently multiplying by zero on every notched/gesture-nav phone, so the iOS home-indicator overlapped the joystick/jump and Android gesture-nav swallowed the hotbar. A new `@media (orientation:landscape)` block then adds the previously-omitted SIDE insets via `calc(<existing px> + env(safe-area-inset-left/right))` for the genuinely edge-hugging fixed elements: `#joyZone`/`#debugHud` (left), `#topBar`/`#btnJump`/`#btnDown`/`#btnMode`/`#btnPing` (right). Portrait keeps today's exact spacing (no rule); desktop is byte-identical (every inset resolves to 0 so the calc collapses to the original px). Zero JS, no new elements; saves/multiplayer/bots/headless untouched.
2. **Touch haptic + crosshair hit-confirmation on break/place/mode/fly/ping.** New `haptic(ms)` (feature-detected, never throws, gated by `IS_TOUCH && hapticsOn`; `navigator.vibrate` is a documented iOS no-op so it degrades to the visual flash there) wired ONLY at real user-input action edges: `completePlayerBreak` (18ms), `doPlace` success (10ms), `btnMode` touchstart (distinct double-tick `[8,40,8]` so Break-vs-Place is FELT), `toggleFly` (longer `[10,30,10,30,25]`), and `nbSendPing` (15ms single pulse). Break/place also pulse the existing `#crosshair` via a transform/opacity-only `.hit` class (scale 1.18 + accent, self-clearing on a single `crosshairFlashTimer` debounce token so hold-to-mine never stacks timers) — the visual half that makes hit-vs-miss unambiguous and is the iOS fallback. NEVER fired per-frame during hold-to-mine (`updateMining` stays silent — only completion edges), and bot.dig / vox dig / `nbApplyRemoteEdit` go through `setBlock` directly so they never touch these functions (verified: every `completePlayerBreak`/`doPlace` call site is a player/touch path). A touch-only `Haptics` checkbox (default ON, `#rowHaptics` revealed only when `IS_TOUCH`) rides the existing settings blob beside `autoQuality`/`cameraMotion` (`haptics` key, same read/write pattern); off silences all pulses and persists. Additive only — no wire/op/save/observation change; `flashCrosshair` is a no-op under headless.
3. **Fixed twin-roster state leak on neighborhood leave (correctness + observation-exfiltration tail).** `nbLeave`'s `wasNeighbor` branch now calls `vfImportRoster(save.twins || [], true)` immediately after the `applyWorldState` success (and `vfImportRoster([], true)` in the no-valid-save fallback) — mirroring the only two other import funnels (`onImportFile`, `voxCmdImport`) which already pass `obj.twins` to the same `replace=true` funnel. Before this fix, leave restored the solo world but never rebuilt the roster, so session twins (and any NETWORK twin started in-session) kept running — and a network twin kept POSTing world observations to its url AFTER the session that scoped its consent ended. The reconcile tears session twins down via `vfRemoveBot` and re-hatches the solo set through the SAME `validateTwinRecord` + consent gate (no new write path, no new trust surface). Guarded to the `wasNeighbor` branch ONLY; the `wasKite` host branch never switched `activeSaveKey`, so its roster is left untouched.

Field notes for future cycles:
- **Deliberate spec deviation (Feature 1):** the original spec listed `#kiteHud left` among the side-inset targets, but `#kiteHud` is CENTERED (`left:50%; transform:translateX(-50%)`) — it hugs no side edge, so adding `env(safe-area-inset-left)` to its `left` would visibly MIS-CENTER it on a notched landscape phone (a regression, not a fix). Per the spec's own "audit the edge each element hugs" rule, the side insets were applied only to genuinely edge-hugging fixed elements; `#kiteHud` (and all other `left:50%` centered HUDs — `#hotbar`, `#toast`, `#questHud`) are intentionally left alone. If a future cycle truly wants centered HUDs nudged off a side notch, do it with a symmetric `padding`/`max-width` clamp, not a one-sided `left` inset.
- **Consent re-gate on leave (Feature 3, documented per spec edge-case a):** a NETWORK twin that was approved+running solo, then survived a join→leave, re-hatches PAUSED if its origin is non-localhost (the cycle-2 consent gate fires on every re-hatch through `vfImportRoster`/`vfHatchTwin`). This is arguably CORRECT — consent is re-confirmed when the world returns to solo — and is the same behavior boot re-hatch already has. Not a regression; the operator resumes via the per-row Resume button or `vox('resume <name>')`.
- **JOIN side intentionally NOT touched this cycle (Feature 3):** while joined, autosave still writes `collectSave()` (incl. `twins: vfRosterData()`) into the `-nb-` session key, so the host key can still accumulate a joiner's twins on a fresh session's autosaves. The leave-side reconcile is the load-bearing fix because the solo `SAVE_KEY` blob is the source of truth on return and is now fully rebuilt from it. A stretch follow-up (have `collectSave` emit an empty `twins` array while `activeSaveKey !== SAVE_KEY`) would stop the session-blob pollution at the source — but it's a deeper change and the leave reconcile is sufficient alone.
- **Haptic gating (Feature 2):** `haptic()` lives inside `nbSendPing`, which is ALSO reached by `vox('ping')`. This is safe because `IS_TOUCH && hapticsOn` gates it — vox typically runs headless/desktop where `IS_TOUCH` is false. If a future cycle ever runs vox on a real touch device and wants a silent programmatic ping, gate the call on an `isUserInput` flag rather than moving it (the spec named `nbSendPing` as the single confirm point).
- The crosshair `.hit` flash is local cosmetic only (transform/opacity, no layout, no frame cost); remote players never see your hit confirmation. The fly-toggle/mode patterns cap every component at ≤40ms per the short-pulse rule.
- Standing wishes still open: block-break progress visible to remote players (dig_progress op, throttled like pos); ore drops / a real inventory; an auto cave-overview map mode; the optional remote-edit RADIUS bound; a Blob-worker clock for full-rate hidden-tab ticking; the JOIN-side session-blob twin-pollution stretch noted above.

## Cycle 8 — 2026-06-12
(The orchestration script labeled this run "cycle 1" of a fresh loop; in this log's numbering it is cycle 8. File now ~7,033 lines, +87 net; both inline scripts pass `node --check`; 450 top-level declarations scanned, 0 duplicates; 92 ids defined, 0 `ui.X` / `getElementById` refs without a matching element. This run is a feel+perf+correctness slate: screen-shake riding the existing camFx accumulator, three near-zero-risk renderer wins, and two verified keydown input-routing defects fixed surgically.)

1. **Screen-shake (trauma) on the existing camFx accumulator.** A RAM-only `trauma` scalar (`[0..1]`, `addTrauma(a)` clamps the sum) feeds a deterministic two-sine-per-axis shake into the SAME `camFx` system that already carries view-bob/dip — so shake writes ONLY `camera.position`/`camera.rotation.z` and PROVABLY never touches `player.pos`/`eyeRay`/DDA targeting (verified: `eyeRay()` reads only `player.pos/eye/yaw/pitch`, never the camera). In `updateCamFx` the bob's existing `±0.0599`/`±0.0119` clamp is applied FIRST, then — only when `cameraMotion && trauma>0` — `shake = trauma*trauma` adds `0.10*sx`/`0.10*sy` to dx/dy and `0.04*sr` to roll (phase `shakeT += dt*22`), followed by a WIDER final clamp (`±0.16`/`±0.05`) gated on `trauma>0` so the shake is actually visible. When `trauma==0` the shake block is skipped and the no-shake path is byte-identical to before; when `!cameraMotion` the bob clamps keep everything 0 and the existing safety belt forces `camFx` to 0 (no-motion path byte-identical). `trauma` decays `dt*1.6`/frame (full decay <1s, can't accumulate into a seizure under rapid mining). Triggers at clean single edges: `completePlayerBreak` 0.12 (0.18 for ore ids 8/9/10), `doPlace` success 0.05, the `updateCamFx` landing edge `0.10 + impact*0.35` (gated by `suppressDipFrames<=0` so a teleport never shakes), `nbSendPing` 0.06. `camFxNotifyTeleport` also zeroes `trauma`. Zero wire/op/save/observation change — bot/remote/vox edits go through `setBlock` directly and never add trauma; remote players never see your shake; headless never reads trauma (the whole `updateCamFx` call is `!voxHeadless`-gated).
2. **Renderer perf slate: far-plane→fog cull + static-chunk `matrixAutoUpdate` off + hard per-frame mesh cap.** (A) `updateFog()` now also pulls `camera.far` down to `ceil(far/16 + 1)*16` (fog.far rounded up + one chunk of margin) and `updateProjectionMatrix()`s, so fully-fogged chunks fall outside the frustum and Three drops them to zero draw calls (camera was created at far=600 while fog.far was only `max(40, renderDist*16-6)`). Runs on every fog-changing path — slider, initial setup, AND world load/import (added one `updateFog()` to `applyWorldState`, idempotent and covering all callers). Near plane (0.1) untouched; the +1-chunk margin keeps the nearest visible chunk well clear of the fog discontinuity at rd2 and rd8. (B) Both chunk-mesh branches (solid + water) in `buildChunkMesh` set `m.matrixAutoUpdate=false; m.updateMatrix()` right after `position.set` — chunk transforms are static and every rebuild re-runs `position.set + updateMatrix`, so a repositioned/rebuilt chunk always refreshes. Scoped STRICTLY to chunk solid/water meshes; avatars/bots/overlays/particles/sky/view-model keep autoUpdate true. (C) A hard cap of `MESH_CAP_PER_FRAME=2` non-local `buildChunkMesh` calls/frame (`meshedThisFrame`, reset at the top of `processStreaming`) layered ON TOP of the 8ms budget, shared across the `meshQueue` streaming loop AND `drainDirtyMeshes`; capped chunks stay in `meshQueue`/`dirtyMeshSet` for the next frame (the drain's cap-check `break`s BEFORE `dirtyMeshSet.delete(k)`, so a capped key is never lost). The local player's own synchronous own-chunk rebuild (`localEditFast` in `setBlock`) is EXEMPT (it never goes through these loops). Cap is in-game only (`!loadingVisible`) so initial world load still floods under its 24ms budget. F3 HUD gains a `meshed/frame` counter. Pure rendering changes — no save/wire/edit-op/observation change.
3. **Fixed panel-close key routing + the bare-Enter chat hijack (two verified keydown defects).** (DEFECT 1) The `KeyH`/`?` close branch now calls `hideHelp(); hideSettings(); hideNb()` (mirroring Escape) — it previously omitted `hideNb()`, so H/? could not close the Neighborhood/kite panel (only Escape could). `hideNb()` is a no-op when nbPanel is already hidden, so adding it is safe. (DEFECT 2) The chat opener was scoped: `KeyT` stays the UNIVERSAL opener whenever chat is reachable (`Neighborhood.enabled || vfBots.length>0`), but BARE `Enter`/`NumpadEnter` now opens chat ONLY when `Neighborhood.enabled` — so hatching a single solo twin no longer hijacks bare Enter (which had been interrupting the cycle-1 onboarding coach and firing right after a native prompt/modal closed). Each branch `return`s, so closing a panel with H never falls through to the chat opener. Chat-send and vox-terminal Enter handling are unaffected (the chat input's own Enter handler `stopPropagation()`s and the top-level listener early-returns on INPUT/TEXTAREA targets). Zero wire/op/save/observation change.

**Implementation notes / deviations:**
- **Trauma decay runs every frame regardless of `cameraMotion`** (it's just never READ unless motion is on). This means toggling motion off mid-shake silently bleeds off any accumulated trauma so re-enabling motion doesn't snap a stale shake back on. The no-motion camera stays perfectly still and byte-identical (the shake block is `cameraMotion`-gated and the safety belt zeroes camFx).
- **`camera.far` math, verified:** rd2 → fog.far=40, camera.far=64 (24-block margin); rd8 → fog.far=122, camera.far=144 (22-block margin). The diagonal corner chunks at rd8 sit ~181 blocks out (past camera.far=144) but they are 100% fogged (>122) and thus invisible — culling them IS the win; nothing partially-visible (<fog.far) is ever cut because camera.far > fog.far by >1 chunk on every renderDist.
- **The mesh cap is shared across both loops by design:** a renderDist increase floods `meshQueue`, an edit burst floods `dirtyMeshSet`, and both should be bounded to 2 rebuilds/frame in-game. During the loading screen the cap is OFF so the world appears fast under the 24ms budget (a 2/frame cap there would add seconds to first paint with nothing to hitch against).

Field notes for future cycles:
- **Trauma is the cheapest possible juice hook** — any future "felt" event (explosion, mob hit, big tower collapse) is one `addTrauma(a)` call at the event edge. Keep magnitudes ≤~0.2 per discrete event; the `dt*1.6` decay + `trauma*trauma` curve already prevents accumulation into a seizure. If a future cycle wants directional shake (e.g. recoil), add a separate biased offset BEFORE the wider clamp — but never feed it back into `player.pos`.
- **The far-plane cull pairs with a future LOD/imposter system:** chunks past fog.far are now frustum-culled but still RESIDENT (meshed) until `unloadFarChunks` drops them by chebyshev distance. A cycle wanting to reclaim that VRAM could dispose meshes (not data) for chunks fully past fog.far — but verify the player can't outrun the re-mesh on a fast fly toward them.
- **The 2/frame mesh cap can leave a visible 1–2 frame fill-in on a big renderDist increase** (chunks pop in a few per frame instead of all at once). This is intentional — it trades a one-time fill animation for a guaranteed hitch-free frame budget. If playtests find the fill too slow at rd8, raise `MESH_CAP_PER_FRAME` to 3–4 (still far below the old unbounded-within-8ms behavior) rather than removing the cap.
- **`matrixAutoUpdate=false` is safe ONLY because `buildChunkMesh` is the single place a chunk transform is set** and it now always calls `updateMatrix()`. A future code path that moves a chunk mesh without `updateMatrix()` would render stale — keep all chunk transform writes inside `buildChunkMesh`.
- **Operators who relied on bare-Enter-opens-chat-with-solo-bots** keep the capability via `KeyT` (T always works with solo twins). Only the bare-Enter hijack is confined to live neighborhood sessions where chat is the expected affordance.
- Standing wishes still open: block-break progress visible to remote players (dig_progress op, throttled like pos); ore drops / a real inventory; an auto cave-overview map mode; the optional remote-edit RADIUS bound; a Blob-worker clock for full-rate hidden-tab ticking; the JOIN-side session-blob twin-pollution stretch; mesh-disposal (not data) for chunks fully past fog.far to reclaim VRAM.

## Cycle 9 — 2026-06-12
(The orchestration script labeled this run "cycle 1" of a fresh loop; in this log's numbering it is cycle 9. File now ~7,512 lines, +479 net; both inline scripts pass `node --check`; 509 top-level declarations scanned, 0 duplicates; 99 ids defined, 93 grabDom registry entries, 0 missing id refs (`ui.X` / `getElementById` all resolve). Three standalone Node harnesses extracted verbatim from the shipped code: spatial-geofence 21/21 (incl. `voxCmdMap` non-slice text + return-JSON byte-diffed against the literal git-HEAD implementation); map+notable+find 23/23; Field Guide 28/28 — 72/72 total. This run closes the cycle's strongest consensus cluster — "cycle-2 caves + cycle-4 ores are invisible to players, twins, AND operators" — plus the longest-deferred remote-edit RADIUS bound (punted in cycle 6, wished in 6/7/8) and the cycle-1 runner-up "floating dynamic joystick + touch comfort settings" that 8 cycles never shipped.)

1. **Surfaced the underground for every audience (three-part bundle).** (A) `vox map` ore glyphs: `CHARS` gained `8:'c' / 9:'i' / 10:'o'` (collide with no existing glyph) and the `'c coal  i iron  o gold'` legend line is appended ONLY inside the existing `if (sliced)` addendum — non-slice text and return JSON verified byte-identical to git HEAD in the harness (ores live in the stone band [2..h-4], so a surface column can never resolve to an ore id). (B) Twin ore perception: module `VF_NOTABLE_IDS=[COAL,IRON,GOLD]` + a new shared pure scanner `vfScanBlocks` (mirrors `bot.findBlocks`' exact bounds — y clamped [0,WORLD_H-1], cube side 2r+1 — `bot.findBlocks` itself untouched); `vfObservation` gained strictly-additive `notable` (radius 10, up to 6 nearest `{block,x,y,z,dist}`, dist integer-rounded, ALWAYS present so the empty case grows only by `"notable":[]`); new `case 'find'` in `vfExecuteOp` (`{op:'find',block,maxDistance?,count?}`, clamps 1..16 / 1..6, defaults 12/3) reporting via `bot.chat` ('found 3 Iron Ore: (x,y,z) …' / 'no Iron Ore within 12' — lands in recentChat via the vfNotifyChat fan-in so the SENDING brain reads its own result next turn) + `voxEvent('find',…)`; unknown block chats 'unknown block', never throws; `vox('find <block> [r]')` CLI command (multi-word names work) returning `{ok,block,found:[{x,y,z}]}`. The twin prompt example + help-overlay op list both advertise `find`. (C) Field Guide: 10-entry `ACHIEVEMENTS` ladder (first_cut, builder 10, architect 50, homesteader 4 distinct chunks via a capped Set, spelunker y<SEA-6, prospector ore break, goldfinger gold, treetop terrain+8, night_owl midnight window, neighbor kite/join edge) persisted as a pure `{id:true}` map at `voxel-world-achievements-v1` (private mode degrades to RAM-only). Hooks ONLY at existing edges — `completePlayerBreak` (knows the broken id, rides next to `coachBreakCount`), `doPlace` success (rides the EXISTING `coachPlaceCount` session counter), kite-up + join-success — plus the spatial/time predicates folded into the EXISTING 1s `fpsAcc` tick, testing only still-locked entries. Unlock = toast('🏅 name') + haptic(15) + addTrauma(0.06) + one sys chat line. A '🏅 N/10' pill (NEW element + CSS copied from #questHud — never the node itself, which the coach REMOVES on finish) sits top-center clear of the touch cluster/hotbar; tap opens a small panel: unlocked names, the NEXT 2 locked hints (deliberately pointing at the unsurfaced content: 'Dig below the waterline to find caves', 'Mine the dark speckled stone'), the rest '???'. Hidden while the coach runs; entirely inert under ?headless=1/?console=1 (achInit mirrors the coach gates: zero DOM writes, zero listeners, zero unlocks, zero events). collectSave/validateSave/wire/roster untouched; pointer lock never stolen.
2. **Spatial anti-grief fence: radius-bound remote edits + bot-act dig/place (the cycle-6 punt, exactly as its field note pre-designed).** Module `nbLastPos` (rappid → {x,z}) updated in the LIVE VoxelFlayer-extended 'pos' handler for NON-BOT payloads only (a twin's broadcast position never moves its owner's box; host relays pos to all leaves, so the map populates at EVERY receiver); cleared in `nbResetSession` alongside `nbEditRx`. In the 'edit' handler, AFTER `nbEditFencePass` and BEFORE the MAX_EDITS ceiling (integer/blockId checks still first): `nbEditGeoPass` drops any edit whose Chebyshev distance `max(|x-lp.x|,|z-lp.z|)` from the sender's last-known pos exceeds `NB_EDIT_RADIUS=96` (~6 chunks, y deliberately ignored) — `nbEditLastAccepted` stays false, so the host neither applies NOR relays (identical drop path to the rate limiter, zero new relay surface). Fallback for a pos-less sender: `nbSpawnRef` = the local player's pos captured at session start (host: kite-up; joiner: every snapshot apply). Local/solo identity NEVER fenced (same rule as nbEditFencePass); last-known pos kept with NO expiry (never reject-all); only ever DROP, never disconnect/toast. Mirrored in 'bot-act': `nbGeoFilterBotActs` drops individual dig/place ops with explicit out-of-box x/z (string coords coerced for the check only); coordless ops and goto/chat/remember/waypoint pass untouched. Observability: `nbEditGeoDrops` counter in `vox status` (`editGeoDrops`) + a ≤1/sec-throttled `voxEvent('edit_geofence',{x,z})`. Harness (verbatim extraction): in-radius accepted incl. the 96 boundary, 97 dropped + not relayed, pos heartbeat moves the box, bot-pos doesn't, spawnRef fallback works, local bypasses, the 60-burst/30-per-sec rate limit and validation order are byte-identical for in-radius edits, a patient 1/sec far attacker is dropped forever — 21/21.
3. **Floating thumb-anchored joystick + look sensitivity slider + invert-Y (cycle-1's never-shipped touch-comfort runner-up).** Floating mode (DEFAULT — the one default change; 'Fixed joystick position' restores the old circle byte-identically): canvasEl touchstart claims a lower-left-region touch (x<45vw, below the 90px+safe-inset top band) BEFORE the look-claim logic — sets `joyTouchId`, records the (safe-area-clamped via a measured-probe `env()` reader) touchdown point as the anchor, repositions #joyZone inline + `.live` opacity fade, and returns WITHOUT setting lookId so look/tap/hold-to-mine (280ms `touchMineTimer`, `lookMoved<14` discrimination) never see it. touchmove drives `joyVec` from the ANCHOR through `joyDriveFrom` — the EXACT existing math (radius 50, same normalization, `joySprint>0.92` edge preserved); touchend/cancel zeroes the vector and fades the ring; `.floating` sets pointer-events:none on #joyZone so its legacy touchstart can never double-claim; a second left-region touch is ignored while held (single-claim guard). Look comfort: `lookSens` (0.4..2.0 step .05, default 1.0, `Number.isFinite`+clamp on load) multiplies BOTH input sites (touch 0.0052×, mouse 0.0023×); `invertY` (default false) flips ONLY the pitch delta sign in both handlers; the PI/2-0.01 clamp untouched. All three persist in the existing settings blob with the same typeof guards as autoQuality/cameraMotion/haptics; `#rowFixedJoy` revealed only when IS_TOUCH (mirrors #rowHaptics); sens+invert show on desktop too. Input-local only — no wire/save/observation change; desktop, headless, bots, multiplayer untouched.

Cycle 9 fixer pass (post-verification, same day): one real major fixed — the spatial fence silently dropped legitimate twin/bot edits, diverging the shared world. A sender's bots legally edit out to renderDist+2 chunks (~111 blocks at default rd4, ~175 at rd8) from their owner's player — past NB_EDIT_RADIUS=96 of the only anchor the fence tracked (player pos heartbeats; the extended 'pos' handler deliberately excluded bot payloads from nbLastPos, and headless console-twin tabs suppress nbSendPos entirely). Edits in that band were applied+persisted at the sender but dropped (no apply, no relay) at the host and every leaf, with zero sender feedback. Fix: per-sender bot anchors (`nbBotPos`: from_rappid → Map(botName → {x,z,t}), capped VF_MAX_BOTS/sender with LRU eviction, cleared in nbResetSession) recorded in the extended 'pos' handler's bot branch — same identity-bound wire as player pos (from_rappid bound at hello), recorded even when the avatar is scene-less/cap-refused, and ordered ahead of any far edit on the same reliable DataConnection (bot pos streams every 0.15s) — and `nbEditGeoPass` now accepts an edit within radius of the player anchor OR any of the sender's bot anchors. A twin's pos still never moves its OWNER's player box; the radius, drop path, never-reject-all and local-bypass rules are unchanged; the headless console-twin tab is now anchored by its twin's pos stream, exactly the presence nbSendPos's suppression comment promised. Harness (verbatim extraction, 24/24): old player-box behavior byte-equivalent at the 96/97 boundary, edit near own bot accepted (the divergence repro), far edits still dropped, headless-sender twin anchored, LRU cap evicts stalest, junk/echo pos never anchors, bot-act mirror inherits the widening, goto untouched.

Cycle 9 fixer pass, second finding (same day): one more real major fixed — the non-slice `vox map` byte-identity guarantee was broken by the shared CHARS map. Feature 1A's safety premise above ("ores live in the stone band [2..h-4], so a surface column can never resolve to an ore id") is FALSE: `genChunk` runs `carveCaves` BEFORE `seedOres`, and the cave-MOUTH pass carves an open shaft from the surface down to a confirmed cave, leaving the cave FLOOR as the column's topmost non-air block — stone in the band [2..h-4] that `seedOres` can then convert to ore (coal y>=12 / iron 6..20 sit right under typical mouth floors). Verified by verbatim Node extraction of the worldgen pipeline: 13 of 1,730,560 columns across 40 seeds x 169 chunks top out at an ore (12 coal, 1 iron; e.g. seed 1 column (-30,52) tops at COAL_ORE y=21) — rendering 'c'/'i' where git HEAD rendered '?'. Fix (minimal): `CHARS` in `voxCmdMap` is now built conditionally — the ore entries 8:'c'/9:'i'/10:'o' exist ONLY when `sliced`; non-slice resolves ids 8-10 to '?' exactly as HEAD did. Harness (voxCmdMap extracted verbatim from BOTH the fixed tree and git HEAD, run over generated worlds containing the ore-topped columns, player offset so the P mark never masks the ore cell): 18/18 non-slice text+return-JSON byte-identity checks at r=2/12/32, zero ore glyphs in any non-slice output, slice mode still renders the glyphs, and the reconstructed PRE-fix shared map provably diverges from HEAD on the repro column (the bug was real, not theoretical). Self-test re-run: both inline scripts pass `node --check`; 489 top-level declarations, 0 duplicates; 99 ids, 0 missing `getElementById` refs. Lesson for future cycles: "topmost block" claims about this terrain must account for cave mouths — any pass that runs after `carveCaves` can surface its block ids through a mouth shaft.

Field notes for future cycles:
- The fence's `nbSpawnRef` is re-captured on EVERY snapshot apply (not just firstJoin) so a host world-replace re-centers the joiner fallback. If a future cycle adds host-side teleport-of-joiners, make sure the teleported client still heartbeats pos (it does — nbSendPos is movement-triggered + 1s idle heartbeat) or its fence box will lag one heartbeat behind.
- `nbEditGeoDrops` is intentionally NOT cleared in nbResetSession (it's a cumulative per-tab operator counter, like a dropped-packets stat). The throttle timestamp `nbEditGeoLastEv` is also global across senders — a flood from two attackers shares one event/sec, which is the point (ring-buffer protection).
- `vfScanBlocks` is a SIBLING of `bot.findBlocks` (returns the matched id + d2), not a replacement — the bot API surface is a protected layer and was left byte-identical. If a future cycle wants `findBlocks` itself to report ids, wrap it rather than fork it.
- obs.notable is ores-only by spec (VF_NOTABLE_IDS). The scanner takes any matchFn — a future 'notable' expansion (water at cave floors? player-placed beacons?) is a one-array change, but remember every addition costs twin tokens on EVERY think.
- The Field Guide's builder/architect counters ride the session-scoped `coachPlaceCount` (only the {id:true} map persists), so 'Architect' wants 50 places in one session — intentional per spec ('persist {id:true} map'). If playtests find that harsh, persist a tiny counters blob beside ACH_KEY — but keep validateSave out of it.
- The achievement pill sits top-center (top:46px+inset) below #kiteHud (top:8, ~36px tall) — if a future cycle grows the kite HUD, nudge the pill, not the kite. The panel (z-36) deliberately sits under #voxTerm (z-45) and the modals (z-50).
- Floating-joystick anchor clamping reads safe-area insets via a probe div + getComputedStyle (env() is CSS-only); the value is cached and refreshed on resize. If a future cycle needs insets elsewhere, lift `readSafeInsets` to module scope rather than re-probing.
- night_owl uses a frac window (0.72..0.78 around midnight=0.75, ~14s wide at DAY_LEN=240) polled at 1Hz — wide enough to never miss, narrow enough to mean 'midnight'. If DAY_LEN ever shrinks below ~60s, re-derive the window.
- Standing wishes still open: block-break progress visible to remote players (dig_progress op, throttled like pos); ore drops / a real inventory (the Field Guide's prospector/goldfinger would pair naturally with drops); an auto cave-overview map mode (the slice glyphs now make slices readable — an operator macro could sweep y=8..20); a Blob-worker clock for full-rate hidden-tab ticking; the JOIN-side session-blob twin-pollution stretch; mesh-disposal past fog.far to reclaim VRAM.

## Cycle 10 — 2026-06-12
(The orchestration script labeled this run "cycle 1" of a fresh loop; in this log's numbering it is cycle 10. File now ~7,948 lines, +390 net; both inline scripts pass `node --check`; 539 top-level declarations scanned, 0 duplicates; 103 ids defined (incl. the JS-created join-HUD nodes), 0 missing `getElementById`/`grabDom`/`querySelector`/`ui.*` refs. Three harnesses run against code extracted VERBATIM from the shipped file (comment-aware extractor at /tmp/extract.py): broker-selection 35/35 (length-2 try sequence byte-diffed against the literal git-HEAD logic for b absent/0/1 across 6 failure patterns; fake length-3 list tries each broker exactly once, lowest-untried-first; b='x'/-1/7/2.5/NaN/1e9 all resolve to 0); worldgen/biome 26/26; feature-1+3 structural 32/32 — 93/93 total.)

1. **QR-arrival join HUD + broker-index/fallback hardening (fused, M4+D1).** (A) `nbJoinWith`'s weak `NB_BROKER_CONFIGS[brokerIdx] !== undefined` check replaced by strict `nbValidBrokerIdx` (integer + range, else 0), both `&b=` parse sites now use `nbParseBrokerParam` (strict `Number` + `Number.isInteger` — `parseInt` would have truncated '2.5' into a *valid* index), and the hardcoded single-flip fallback became a `triedIdx` Set seeded with the link index: each fallback-eligible failure (primary 13s watchdog OR a terminal `NB_JOIN_BROKER_ERRS` error — NEVER 'peer-unavailable', cycle-6 semantics preserved) tries the lowest untried broker, at most one attempt per broker. Byte-equivalent to HEAD with today's 2-entry list (harness-diffed); ready for 3+. `vox('status')` gains `broker: n` (set at every join attempt and at kite-up). (B) `nbMaybeAutoJoin` now shows a centered card (`#nbJoinHud`, z-44 below voxTerm/modals, pointer-events only on the card, pure-CSS spinner, safe-area padded) instead of the old Neighborhood modal — status driven ONLY by existing lifecycle hooks: attempt start → 'Reaching the kite…', fallback → 'Trying another broker…', peer `open` (where wd2 arms) → 'Handshaking…', snapshot success → "You're in <host>'s world!" (folds the old 2s joiner toast — never double-shown) with ~1.5s auto-dismiss, terminal failure → RETRY (fresh `triedIdx` via re-running `nbJoinWith`, disabled in-flight) + 'Play solo instead' (≥44px buttons). Mid-attempt 'Play solo' calls `nbJoinAbort` — the SAME cleanup the watchdogs use (clear both watchdogs + `peer.destroy()` + `nbResetSession`), and it no-ops once `Neighborhood.enabled`, so a late snapshot can never apply over the restored solo world. PeerJS-CDN-failure renders the offline note + 'Play solo' (no spinner); pre-snapshot conn close in `nbOnHostLost` also surfaces the failure state (no hung spinner). Card suppressed entirely under `?headless=1`/`?console=1`; manual panel joins and `vox('join')` never see it (`nbJoinHudSet` no-ops while the card is absent). Node removed (not hidden) on dismiss. Zero wire/op/save changes.
2. **Deterministic biome layer (temperature × moisture) + SNOW block 11.** `tempAt`/`moistAt` = fbm2 at salts 0x7e3b/0x1c9d, freq 0.0021 (~480-block regions); `biomeAt` = rocky elevation override first, then desert (t>0.62 && m<0.40) / snowy (t<0.36) / forest (m>0.60) / plains. `terrainHeightAt` NEVER touched — heights/caves/mouths/ore bands/spawn math byte-identical. Cap repaint runs as a new `paintBiomeCaps` post-pass in genChunk AFTER carveCaves+seedOres, BEFORE trees (carve still sees vanilla caps → the cycle-2 no-daylight invariant holds — harness: 0 holes over 12 seeds × 25 chunks incl. 1,511 rocky columns; ore bands [2..h-4] are disjoint from the cap [h-3..h] by construction): only columns whose top still holds the ORIGINAL grass cap repaint (mouth shafts/belled lips skip exactly where the carve stopped); desert → grass+dirt→SAND ×4, snowy → grass→SNOW ×1, rocky → top 1-2 →STONE; beaches untouched; temp/moist sampled once per column; edits overlay still applies last. Trees gated INSIDE `treeInfoAt` (cheap roll cull first, so the fbm2 cost hits <2% of calls): forest 0.018, plains 0.0065, desert/snowy/rocky none — findSpawn/spawnHeadClear/mouth pass follow automatically (200-seed sweep green: no mouth spawns, head clear, solid footing). SNOW = id 11: B/BLOCK_NAMES/COLORS/TILES (atlas slot 16, white + blue-grey speckle drawn AFTER the ore tiles so the shared rng stream leaves every existing tile byte-identical)/hardness 0.3/PLACEABLE (hotbar slot 7, help text updated to 1–7)/ICON_TILES; MAX_BLOCK_ID bumped to B.SNOW — validateSave and the wire edit check follow from the single source (harness: id 11 accepted, 12 rejected, old saves still pass). Hotbar icon read hardened to `ICON_TILES[id] ?? BLOCK_TILES[id].side` (D4). `vox map` BASE map gains `11:'*'` + legend (snow is a legitimate surface id — intended output change; ores stay slice-gated per the cycle-9 lesson; harness: every '*' sits on a real SNOW cap and vice versa). `vfObservation` gains `biome` (one string, additive). Distribution measured: plains 59% / forest 20% / snowy 14% / desert 5% / rocky 2% — plains+forest 79%, all 5 biomes on all 8 sampled seeds within ±1200. genChunk avg 0.10ms incl. the new pass (80x inside the 8ms budget).
3. **First-person held-block viewmodel with swing (G1).** A second tiny `handScene` (one BoxGeometry(0.5) MeshLambert mapped to the EXISTING atlasTexture — no new upload — plus one Directional + one Hemisphere light) rendered as an overlay pass inside the same `!voxHeadless && !document.hidden` guard right after the main render: `autoClear=false; clearDepth(); render(handScene, handCam); autoClear=true` — terrain can never clip through, main pass untouched, 1 extra draw call at the governed renderScale. Own PerspectiveCamera (fov 60, near 0.01, far 5), aspect synced in the existing resize handler; mesh parks at (0.42,−0.38,−0.72) rot(−0.1,−0.35). Per-face UVs from BLOCK_TILES via `tileUV` (BoxGeometry face order +x,−x,+y,−y,+z,−z; faces 2/3 = top/bottom), rebuilt ONLY on selSlot change (crackMesh pattern) — SNOW shows up automatically. `swingT` (module-level, restart-to-1) fires ONLY at the crack-stage-advance edge in `applyMineDamage` and on `doPlace` success, eases to 0 over 0.22s in `updateCamFx`, drives a mesh-local chop arc (pitch −0.9·sin(πt), y −0.12, z −0.05); idle sway rides the EXISTING `bobPhase` (no second phase) scaled by speed, gated on cameraMotion; hand lights copy the day/night daylight factor each frame. 'First-person hand' checkbox (`showHand`, default ON, typeof-guarded like cameraMotion) hides the mesh AND skips the pass. Structurally proven (harness greps over the extracted fns): hand code never writes `camera.*`/`player.*`/`bobPhase`, never feeds camFx — DDA targeting byte-identical hand on/off; `ensureHandScene` refuses under `?headless=1`/`?console=1` (zero objects, zero pass). 'become' mode shows the PLAYER's selected block by design.

**Spec deviations (deliberate, measured):**
- Feature 2's rocky override was specified as `terrainHeightAt > SEA+22` (=48), but this terrain's heights top out at ~47 in practice (h≥48 measured at 0.000% over 2.9M columns across 8 seeds) — the biome would have been stillborn and the DoD's "all 5 biomes occur" unsatisfiable. Shipped `h > SEA+11` (~1.7% of columns: genuine hilltops). Temp/moist thresholds kept exactly as specified (they measured 79% plains+forest ≥ the 60% floor with all 5 biomes present per-seed).
- Feature 1's spec line "the &b= parse site coerced to integer" was implemented as a strict `Number`+`Number.isInteger` parse rather than `parseInt`, because the DoD requires b='2.5' → 0 and `parseInt('2.5')`=2 would VALIDLY index a 3-entry list. The old `parseInt(...)||0` sites are both replaced by the shared `nbParseBrokerParam`.
- The auto-join path no longer opens the Neighborhood modal (`showNb()`) — the card replaces it per the spec's "show a centered card" UX; manual joins still use the panel exactly as before.

Cycle 10 fixer pass (post-verification, same day): one real major fixed — 'Play solo instead' tapped during the PRE-attempt async window did not abort the join. `nbMaybeAutoJoin` shows the card (live 'Play solo instead' button from the first 'connecting' render) BEFORE `nbJoinWith` runs, but `nbJoinAbort` is only armed inside `attempt()`, which sits behind two awaits: `nbEnsureLibs()` (PeerJS/QRious CDN loads — seconds on a first-visit phone, exactly the QR-arrival case) and `nbDeriveKey()` (PBKDF2 210k iterations). A tap in that window hit `nbJoinHudDismiss(true)` with `nbJoinAbort === null` — card removed, nothing aborted — and the join kept running headless: `attempt()` later re-armed an unreachable `nbJoinAbort`, and `nbApplySnapshot` (gated only on role+validateSave) replaced the solo world the user explicitly chose (the success path even toasted "You're in X's world" via the no-card else-branch). Fix (minimal, flag-based per the finding's primary suggestion): module-level `nbJoinCancelled` set by `nbJoinHudDismiss(true)` ONLY when `nbJoinAbort` is null (mid-attempt dismissal still takes the abort path, never the flag); `nbJoinWith` clears it at entry (covers RETRY, which re-runs `nbJoinWith`) and checks it after EACH of the two awaits, bailing with `return false` before `attempt()` — the bail mirrors `nbJoinAbort`'s end state (`nbResetSession()` clears the just-set token/key) and reuses its `Neighborhood.enabled` guard so a session the user started during the window is never torn down. No wire/op/save change; manual panel joins and `vox('join')` never have a card so the flag never fires there. Harness (nbJoinWith + nbJoinHudDismiss + the three nbJoin* declarations extracted verbatim, controllable awaits): cancel-during-CDN and cancel-during-PBKDF2 both bail pre-attempt with clean reset; happy path arms nbJoinAbort; stale flag never kills a RETRY; mid-attempt dismiss unchanged (peer.destroy + reset, flag untouched); cancel-then-user-kites-up bails without resetting the live session — 15/15. Self-test re-run: both inline JS scripts pass `node --check` (importmap block is JSON, validated separately); 540 top-level declarations, 0 duplicates.

Cycle 10 fixer pass 2 (same day): one more real major fixed — the joiner's `conn 'close'` handler was asymmetric with `conn 'error'`: it called `nbOnHostLost()` directly without settling the attempt or clearing watchdogs. A pre-`conn open` close (PeerJS emits this when the host peer dies mid-negotiation / ICE closes) ran nbOnHostLost's pre-handshake branch (nbResetSession + 'failed' card) while leaving the per-attempt `settled` flag false and the 8s wd2 (armed at peer `open`) still pending. The new one-tap RETRY makes a <8s re-attempt the designed flow: the stale wd2 then fired `failOrFallback(false)`, passed the `if (settled) return` guard (per-attempt closure), and its terminal branch ran `nbResetSession()` — which destroys `Neighborhood.peer` UNCONDITIONALLY (the `=== peer` guard only protects nulling the variable), tearing down the retry's fresh peer or even a fully-joined session (stranding the player on the `-nb-` activeSaveKey with the HUD reading 'failed'), violating the invariant comment at failOrFallback's head. Fix (minimal, the finding's own suggestion): `conn.on('close')` now mirrors `conn.on('error')` — `if (!settled) { failOrFallback(false); return; } nbOnHostLost();` — failOrFallback sets settled=true and clears both watchdogs, so no stale timer can survive into a retried attempt; post-handshake closes still take nbOnHostLost exactly as before. Minor surface change: a pre-handshake close now shows failOrFallback's 'Could not reach the kite' status/toast instead of nbOnHostLost's 'closed before the handshake finished' line — same terminal 'failed' card either way. Self-test re-run: both inline JS scripts pass `node --check`; importmap block validated as JSON; file 7,982 lines.

Field notes for future cycles:
- Existing players' worlds regenerate under the same seed with biomes (precedented by cycles 2/4): surroundings change, builds survive via the edits overlay. A stale-cached old client joining a new host sees divergent terrain exactly as when caves/ores shipped — same no-mitigation call, noted per spec.
- Tree gating changed `treeInfoAt`, which `caveMouthAt` consults (`never under a tree`), so mouth DISTRIBUTION shifts slightly in ex-tree/new-tree columns — the mouth↔cave-confirmation logic itself is untouched and the no-blind-pits/no-daylight invariants re-verified green.
- `nbJoinAbort` is assigned per attempt inside `nbJoinWith` even for manual/vox joins (harmless — only the HUD's 'Play solo' button ever calls it, and it's nulled on success/terminal/dismiss). If a future cycle adds another join-cancel surface, reuse this hook rather than reaching into the peer.
- The join HUD's RETRY reuses `nbJoinParams` (a const snapshot of the boot URL) — a host whose peer-id rotated needs a fresh link; retry intentionally targets the same kite.
- The hand viewmodel renders AFTER `updateCamFx` ran in the same frame (gameTick order), so swing decay and pose read this frame's state; if a future cycle adds more viewmodel items (tools?), extend `setHandBlock`'s per-face mapping rather than adding meshes — one draw call is the budget.
- Atlas slots 17-63 remain free. The snow tile draws after the ore tiles on the SHARED mulberry32 stream — any future tile must also append after slot 16's draw call, or every earlier procedural tile shifts appearance.
- Hotbar is now 7 slots; the digit-key handler and wheel cycling are PLACEABLE.length-driven and followed automatically. At 8+ slots, check the 360px-wide phone fit (42px slots + gaps ≈ 47px each).
- Standing wishes still open: block-break progress visible to remote players (dig_progress op, throttled like pos); ore drops / a real inventory; an auto cave-overview map mode; a Blob-worker clock for full-rate hidden-tab ticking; the JOIN-side session-blob twin-pollution stretch; mesh-disposal past fog.far to reclaim VRAM; biome-aware ambient audio (wind on rocky/snowy, birds in forest) would pair naturally with the cycle-3 footstep synth.

## Cycle 11 — 2026-06-12

(Log entry backfilled 2026-06-16: the cycle-11 code shipped in commit `a6f28ad` on 06-12 23:16 — recovered and committed after the original 24h loop run hit the monthly spend limit mid-implement — but the log append never ran. Reconstructed from the shipped diff; both inline scripts pass `node --check`; file 8,370 lines. Note: the orchestrator's working title for this cycle differed from the slate that actually shipped — the three features below are the source of truth.)

1. **Biome- and time-aware ambient audio bed.** A dedicated ambient bus (`ambMasterGain`) hangs off the existing AUDIO toggle, so `soundOn=false`, hidden tab, and headless all silence the bed alongside every other sound (`ambSilence`). A 1Hz mood selector (`ambTick`, never per-frame) chooses the layer set from the player's current biome + day/night phase — directly cashing the cycle-10 field-note prediction ("biome-aware ambient audio … would pair naturally with the cycle-3 footstep synth"). Build is wrapped so a WebAudio failure sets `ambBuildFailed` and the game simply runs with no bed (never retry-throws). Zero new wire ops and zero observation fields — multiplayer peers never hear your ambient, so it can't desync or leak.
2. **Mixed-content handling for network twins.** On an HTTPS page (e.g. GitHub Pages), the browser blocks a `fetch` to an `http://` twin endpoint before any request leaves — previously a silent, confusing twin stall. Now `vfUrlBlocked` detects the `http:`-from-`https:` case, returns WITHOUT fetching, fires a one-time `toast` + `twin-turn {ok:false, error:'mixed-content'}` vox event (gated by `_mixedWarned`), and surfaces a `warning` field in `vox status`. `http://localhost` stays exempt so the working Tethered-brainstem path keeps functioning from an https page; pages served over plain `http://` are never affected.
3. **Remote avatar walk-bob.** Remote player/twin avatars now carry `walkPhase` + `prevX/prevZ`; per frame their horizontal speed drives `walkPhase += sp*dt*7` and a `bobY = |sin(walkPhase)| * 0.05` vertical bob (only above a 0.3 speed threshold), so neighbors visibly stride instead of gliding. Pure presentation on the receiving side — no new wire data, reuses the existing lerped pos stream.

Field notes for future cycles:
- The ambient bed is local-only by design; if a future cycle wants shared ambience it needs a new throttled op, not a broadcast of the raw audio state.
- `vfUrlBlocked` only guards the `https→http` mixed-content case; an `https→https` twin endpoint that refuses CORS still surfaces through the normal fetch-error backoff path, not this one-time note.
- Standing wishes still open (carried from cycle 10): remote-visible block-break progress; ore drops / inventory; auto cave-overview map mode; Blob-worker clock for full-rate hidden-tab ticking; mesh-disposal past fog.far to reclaim VRAM.
