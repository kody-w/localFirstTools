{
  "schema": "localfirst-failure-genome/v1",
  "cases": [
    {
      "id": "replay-argument-contract-drift",
      "status": "fixed",
      "observed_stage": "scheduler validation after successful source qualification",
      "symptom": "Both real source projects qualified, but the scheduler rejected the packager's standalone --replay and --allow-checks flags.",
      "cause": "The scheduler treated every verification argument as a flag/value pair; its fixture omitted the real boolean flags.",
      "correction": "Bind the exact canonical verifier argv, including source checkout, report, capsule, replay and explicit process permission.",
      "regression": "tests/test_capability_scheduler.py: test_invalid_reports_never_reach_replay",
      "acceptance": [
        "The real packager's replay command is admitted.",
        "Missing --replay or --allow-checks is refused.",
        "The same real two-project plan completes after the corrected contract."
      ],
      "lesson": "Producer and consumer fixtures must share the actual executable contract; component-level green results are not integrated compatibility."
    },
    {
      "id": "public-evidence-fixture-compatibility",
      "status": "fixed",
      "observed_stage": "RAPP publication after successful qualification",
      "symptom": "The public-artifact guard refused a test source file containing a synthetic secret-shaped literal.",
      "cause": "The evaluator's fixture was safe as a test input but incompatible with preserving the evaluator itself as a public source artifact.",
      "correction": "Keep the guard unchanged; generate clearly synthetic refusal input at runtime and repin the changed evaluator revision before retrying.",
      "regression": "tests/test_capability_package.py: test_traversal_private_binary_and_colliding_paths_are_refused",
      "acceptance": [
        "Secret-shaped inputs remain refused.",
        "All declared public capability and evaluator artifacts can be preserved without a secret exception.",
        "The changed manifest and explicit policy use a new revision; exhausted attempts are not erased."
      ],
      "lesson": "A durable capability includes usable evaluation assets; qualification must extend through the actual evidence-publication boundary."
    },
    {
      "id": "help-is-not-a-qualification",
      "status": "fixed",
      "symptom": "A verifier command with --help can exit zero without exercising the qualification.",
      "correction": "All producers and consumers share one exact replay contract that rejects help, extra or duplicate options, and option-like argument values.",
      "regression": "tests/test_capability_registry.py: test_help_only_replay_cannot_promote_even_when_its_process_exits_zero",
      "lesson": "A successful process exit must be tied to the intended operation, not merely the expected executable name."
    },
    {
      "id": "dependency-evidence-closure",
      "status": "fixed",
      "symptom": "A primary capability can complete while its dependency manifests and helper bytes are absent from the recorded evidence.",
      "correction": "Carry, revalidate and attest the complete transitive dependency closure; enforce recorder limits on the complete set.",
      "regression": "tests/test_capability_scheduler.py: test_dependency_manifests_and_artifacts_are_recorded_with_the_job",
      "lesson": "Reuse edges must bind executable dependencies through qualification and publication, not stop at planning."
    },
    {
      "id": "exhausted-work-must-not-starve-new-work",
      "status": "fixed",
      "symptom": "A retained plan repeatedly selects an exhausted or interrupted job before an eligible job and spends its whole execution budget refusing the first item.",
      "correction": "Determine eligibility before allocating execution slots; report blocked, busy and recovery-required work without consuming the run budget.",
      "regression": "tests/test_capability_scheduler.py: test_exhausted_job_does_not_starve_later_eligible_work",
      "lesson": "Failure retention and progress are compatible only when refusal states cannot monopolize the scheduler."
    },
    {
      "id": "qualification-must-use-the-pinned-source-not-the-current-checkout",
      "status": "fixed",
      "symptom": "The source capsule was correctly built from its selected Git commit, but qualification compared it to newer checkout files and failed after the repository evolved.",
      "correction": "Compare source content with the selected immutable Git objects. Continue checking executing capability artifacts against current manifest pins and restored output against capsule bytes.",
      "regression": "tests/test_capability_package.py: test_qualification_replays_the_pinned_commit_not_current_checkout_files",
      "lesson": "A replayable artifact must not silently depend on unrelated current working-tree contents."
    }
  ],
  "limitations": [
    "These are observed engineering failures, not claims about unrelated applications.",
    "No sensitive fixture value is reproduced here.",
    "A failure remains identified until its end-to-end acceptance has completed."
  ]
}
